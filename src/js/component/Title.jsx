import React from "react";

const Title = ({ title, nameBtn }) => {
  return (
    <>
      <div className="text-center mt-5">
        <h1>{title}</h1>
      </div>
    </>
  );
};

export default Title;
